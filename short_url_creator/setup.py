#!/usr/bin/env python


from setuptools import find_packages
from distutils.core import setup

setup(name='short_url_creator',
      version='0.1',
      description='',
      packages=find_packages())
